# Build artifacts
output/
output_ref/
.spago/
.psc-ide-port
*.log
node_modules/

# Per-subproject build artifacts (prelude-pool also has its own)
prelude-pool/output/
prelude-pool/output_ref/
prelude-pool/.spago/

# Editor scratch
.DS_Store
.idea/
.vscode/
*.swp
*~

# spago.lock is checked in at the top level (matches a reproducible build),
# but generated subprojects' lockfiles are not.
prelude-pool/spago.lock
